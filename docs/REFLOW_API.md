# Reflow Core API consumption

> Filled out in Phase I. Will enumerate exactly which upstream Reflow Core
> endpoints the connector consumes (document submit, status poll, PII
> approve/deny, presigned S3 URL fetch).
