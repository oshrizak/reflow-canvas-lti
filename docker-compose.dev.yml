services:
  connector:
    volumes:
      - ./connector:/app/connector
    command: >
      uvicorn connector.main:app
      --host 0.0.0.0
      --port 8000
      --reload
      --reload-dir /app/connector
